<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.tensorflow.lite.examples.poseestimation">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/renault"
        android:label="RenaultApp"
        android:roundIcon="@drawable/renault"
        android:supportsRtl="true"
        android:theme="@style/Theme.PoseEstimation">
        <activity android:name=".LiveActivity">

        </activity>

        <activity android:name=".LiveActivity2">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".VideoActivity"></activity>
    </application>

</manifest>